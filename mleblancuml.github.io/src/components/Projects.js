import { Container } from "@mui/material";

export default function Projects() {
    return (
        <Container sx={{ justifyContent: "center", textAlign: "center" }}>
            <h1>Page containing my projects!</h1>
        </Container>
    );
}